node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

.env
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Supabase local CLI state
supabase/.branches
supabase/.temp

# Generated env bundle for the Vercel dashboard — contains real secrets.
vercel-env.txt

# Real placement records loaded into Databricks. Aggregate-safe by design, but
# they are the placement cell's data, not the repo's — keep the template only.
databricks/data/placements.csv
